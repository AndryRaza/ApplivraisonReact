const initialState = { panier: [] }

function addPack(state = initialState,action)
{
    let nextState
    switch(action.type){
        case 'ADD':
            return nextState
        default:
            return state
    }
}